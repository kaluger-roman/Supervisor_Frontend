export enum Pages {
    keypad = "keypad",
    call = "call"
}

export enum CallPages {
    none = "none",
    call = "call",
    ringingInbound = "ringingInbound",
    waitingOutbound = "waitingOutbound"
}
